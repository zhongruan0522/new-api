---
alwaysApply: false
globs: "frontend/tests/**/*.ts"
---

# E2E Testing Rules

1. Add `data-testid` for robust element selection.
2. Use `my@example.com` as the email and `pwd123456` as the password for login when testing the frontend.
