package bytedance

// ModelList intentionally empty: ByteDance (Volcengine Ark) endpoints are
// configured per-account via model endpoints (e.g. ep-xxxxx). Users add the
// models they enabled on the channel manually.
var ModelList = []string{}

var ChannelName = "bytedance"
