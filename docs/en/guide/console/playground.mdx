---
title: Playground
---

The `/playground` route is used to test model calls directly in the console.

Common uses:

- Verify that API keys, groups, and models are available.
- Debug streaming output, model parameters, and multi-turn conversations.
- Compare responses across models or channels.

Playground is for interactive testing only; production calls should still use API keys through a compatible API endpoint.
