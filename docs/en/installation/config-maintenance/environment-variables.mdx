---
title: Environment Variable Configuration Guide
---

This document lists common NookMux environment variables and their defaults. The complete list is defined in `.env.example` at the repository root, and the backend parsing logic lives in `internal/common/init.go`, `internal/common/redis.go`, `internal/model/main.go`, and related files.

<Callout type="info" title="Tip">
  NookMux can read environment variables from the project root `.env` file. Copy `.env.example` to `.env` and adjust it as needed. Unset variables use the defaults shown below.
</Callout>

## Basic Configuration

| Environment Variable | Description | Default Value | Example |
| --- | --- | --- | --- |
| `PORT` | Service listening port, also available via the `--port` startup flag | `3000` | `PORT=8080` |
| `TZ` | Time zone | - | `TZ=Asia/Shanghai` |
| `VERSION` | Override the running version number, which defaults to the build-time git commit | Build value | `VERSION=1.2.3` |
| `GIN_MODE` | Gin runtime mode; set it to `release` to disable debug logs | - | `GIN_MODE=release` |
| `FRONTEND_BASE_URL` | Only effective on slave nodes; non-API routes are redirected to this address | - | `FRONTEND_BASE_URL=https://your-domain.com` |

## Real Client IP (Reverse Proxy / CDN)

| Environment Variable | Description | Default Value |
| --- | --- | --- |
| `TRUSTED_PROXIES` | Comma-separated list of trusted proxy IPs/CIDRs. Headers such as `CF-Connecting-IP` and `X-Forwarded-For` are only parsed when the request comes from a trusted proxy. Special values: `private`/`local`, `cloudflare`/`cf`, `all`/`unsafe`, `none`/`disable` | `private,cloudflare` |

## Database Configuration

| Environment Variable | Description | Default Value | Example |
| --- | --- | --- | --- |
| `SQL_DSN` | Database connection string; SQLite is used when unset | SQLite (`data/one-api.db`) | MySQL: `SQL_DSN=user:password@tcp(127.0.0.1:3306)/dbname?parseTime=true`; PostgreSQL: `SQL_DSN=postgres://user:password@127.0.0.1:5432/dbname` |
| `LOG_SQL_DSN` | Separate database connection string for log tables; leave empty to share the main database | - | `LOG_SQL_DSN=user:password@tcp(127.0.0.1:3306)/logs` |
| `SQLITE_PATH` | SQLite database file path | `data/one-api.db` | `SQLITE_PATH=/var/lib/nookmux/nookmux.db` |
| `SQL_MAX_IDLE_CONNS` | Maximum number of idle connections in the pool | `20` | `SQL_MAX_IDLE_CONNS=50` |
| `SQL_MAX_OPEN_CONNS` | Maximum number of open connections in the pool | `1000` | `SQL_MAX_OPEN_CONNS=500` |
| `SQL_MAX_LIFETIME` | Maximum connection lifetime (seconds) | `60` | `SQL_MAX_LIFETIME=120` |
| `SQL_PREPARE_STMT` | Enable GORM prepared statement cache for hot SQL paths | `false` | `SQL_PREPARE_STMT=true` |

## Cache and Redis Configuration

| Environment Variable | Description | Default Value | Example |
| --- | --- | --- | --- |
| `REDIS_CONN_STRING` | Redis connection string; Redis is disabled when unset | - (disabled) | `REDIS_CONN_STRING=redis://:password@localhost:6379/0` |
| `REDIS_POOL_SIZE` | Redis connection pool size | `50` | `REDIS_POOL_SIZE=20` |
| `REDIS_MIN_IDLE_CONNS` | Minimum number of idle Redis connections | `5` | `REDIS_MIN_IDLE_CONNS=2` |
| `REDIS_DIAL_TIMEOUT` | Redis connection timeout (seconds) | `5` | - |
| `REDIS_READ_TIMEOUT` | Redis read timeout (seconds) | `3` | - |
| `REDIS_WRITE_TIMEOUT` | Redis write timeout (seconds) | `3` | - |
| `REDIS_MAX_RETRIES` | Redis command retry count | `3` | - |
| `MEMORY_CACHE_ENABLED` | Enable in-process memory cache | `false` | `MEMORY_CACHE_ENABLED=true` |
| `SYNC_FREQUENCY` | Cache and database sync frequency (seconds) | `60` | `SYNC_FREQUENCY=60` |
| `BATCH_UPDATE_ENABLED` | Enable batch database update aggregation | `false` | `BATCH_UPDATE_ENABLED=true` |
| `BATCH_UPDATE_INTERVAL` | Batch update aggregation interval (seconds) | `5` | `BATCH_UPDATE_INTERVAL=10` |

<Callout type="info">
  Setting `REDIS_CONN_STRING` automatically enables the memory cache, which matches the previous behavior.
</Callout>

## Multi-Node and Security Configuration

| Environment Variable | Description | Default Value | Example |
| --- | --- | --- | --- |
| `SESSION_SECRET` | Session secret. It must be set for multi-node deployments and must be identical on every node. If set to `random_string`, startup fails immediately. | - | `SESSION_SECRET=<random>` |
| `COOKIE_SECURE` | Whether the session cookie carries the `Secure` flag (sent only over HTTPS). Must be enabled for HTTPS deployments (or when a reverse proxy terminates TLS), otherwise the long-lived session cookie may leak over plaintext HTTP channels | `false` | `COOKIE_SECURE=true` |
| `CRYPTO_SECRET` | Encryption secret used to encrypt sensitive database content. If unset, it reuses `SESSION_SECRET` | `SESSION_SECRET` | `CRYPTO_SECRET=<random>` |
| `NODE_TYPE` | Node type. Nodes set to `slave` skip database migrations, root account creation, and scheduled channel checks | `master` | `NODE_TYPE=slave` |

<Callout type="info" title="Cluster Deployment">
  For a full cluster deployment using these variables, see the [Cluster Deployment Guide](/en/docs/installation/deployment-methods/cluster-deployment).
</Callout>

## User and Token Configuration

| Environment Variable | Description | Default Value | Example |
| --- | --- | --- | --- |
| `GENERATE_DEFAULT_TOKEN` | Generate an initial token for newly registered users | `false` | `GENERATE_DEFAULT_TOKEN=true` |
| `NOTIFICATION_LIMIT_DURATION_MINUTE` | Notification limit duration (minutes) | `10` | `NOTIFICATION_LIMIT_DURATION_MINUTE=15` |
| `NOTIFY_LIMIT_COUNT` | Maximum number of notifications within the specified duration | `2` | `NOTIFY_LIMIT_COUNT=3` |

## Request and Response Limit Configuration

| Environment Variable | Description | Default Value | Example |
| --- | --- | --- | --- |
| `GLOBAL_API_RATE_LIMIT_ENABLE` | Global API rate limit switch | `true` | `GLOBAL_API_RATE_LIMIT_ENABLE=false` |
| `GLOBAL_API_RATE_LIMIT` | Global API rate limit count per IP | `180` | - |
| `GLOBAL_API_RATE_LIMIT_DURATION` | Global API rate limit window (seconds) | `180` | - |
| `GLOBAL_WEB_RATE_LIMIT_ENABLE` | Global Web rate limit switch | `true` | - |
| `GLOBAL_WEB_RATE_LIMIT` | Global Web rate limit count per IP | `60` | - |
| `GLOBAL_WEB_RATE_LIMIT_DURATION` | Global Web rate limit window (seconds) | `180` | - |
| `CRITICAL_RATE_LIMIT_ENABLE` | Critical operation rate limit switch | `true` | - |
| `CRITICAL_RATE_LIMIT` | Critical operation rate limit count | `20` | - |
| `CRITICAL_RATE_LIMIT_DURATION` | Critical operation rate limit window (seconds) | `1200` | - |
| `SEARCH_RATE_LIMIT_ENABLE` | Search API rate limit switch | `true` | - |
| `SEARCH_RATE_LIMIT` | Search API rate limit count per IP | `10` | - |
| `SEARCH_RATE_LIMIT_DURATION` | Search API rate limit window (seconds) | `60` | - |
| `RELAY_TIMEOUT` | Relay request timeout (seconds); `0` means no limit | `0` | `RELAY_TIMEOUT=60` |
| `RELAY_MAX_IDLE_CONNS` | Maximum idle connections for the relay HTTP client | `200` | - |
| `RELAY_MAX_IDLE_CONNS_PER_HOST` | Maximum idle connections per host for the relay HTTP client | `32` | - |
| `RELAY_IDLE_CONN_TIMEOUT` | Idle connection retention time for the relay HTTP client (seconds) | `90` | - |
| `STREAMING_TIMEOUT` | Streaming single-reply timeout (seconds) | `300` | `STREAMING_TIMEOUT=120` |
| `STREAM_SCANNER_MAX_BUFFER_MB` | Maximum buffer size for a single streaming SSE frame (MB) | `8` | - |
| `MAX_FILE_DOWNLOAD_MB` | Maximum file download size (MB) | `64` | `MAX_FILE_DOWNLOAD_MB=50` |
| `MAX_REQUEST_BODY_MB` | Maximum request body size (MB, counted after decompression) | `128` | `MAX_REQUEST_BODY_MB=100` |
| `ANONYMOUS_REQUEST_BODY_LIMIT_KB` | Anonymous request body size limit (KB) | `512` | - |
| `MAX_RESPONSE_BODY_MB` | Baseline upstream response body limit (MB) | `16` | - |
| `MAX_TEXT_RESPONSE_BODY_MB` | Text response body limit (MB) | Same as `MAX_RESPONSE_BODY_MB` | - |
| `MAX_EMBEDDING_RESPONSE_BODY_MB` | Embedding response body limit (MB) | `64` | - |
| `MAX_MEDIA_RESPONSE_BODY_MB` | Media response body limit (MB) | `64` | - |
| `MAX_ERROR_RESPONSE_BODY_MB` | Error response body limit (MB) | `4` | - |
| `MAX_MODEL_LIST_RESPONSE_BODY_MB` | Model list response body limit (MB) | `8` | - |

<Callout type="warn" title="RELAY_TIMEOUT warning">
  Be careful when setting `RELAY_TIMEOUT`: a too-short timeout can let the upstream API finish and bill successfully while the local side times out before billing completes, causing billing desynchronization. Keep the default `0` unless you know exactly why you are changing it.
</Callout>

## Channel Management Configuration

| Environment Variable | Description | Default Value | Example |
| --- | --- | --- | --- |
| `CHANNEL_UPDATE_FREQUENCY` | Periodic channel balance update frequency (minutes); disabled when unset | - | `CHANNEL_UPDATE_FREQUENCY=1440` |
| `CHANNEL_TEST_FREQUENCY` | Periodic channel health check frequency (minutes); disabled when unset | - | `CHANNEL_TEST_FREQUENCY=1440` |
| `POLLING_INTERVAL` | Request interval when batch updating channels (seconds) | `0` | `POLLING_INTERVAL=5` |

## Model and Request Processing Configuration

| Environment Variable | Description | Default Value | Example |
| --- | --- | --- | --- |
| `FORCE_STREAM_OPTION` | Override the client's `stream_options` parameter to force usage output | `true` | `FORCE_STREAM_OPTION=false` |
| `CountToken` | Count text tokens | `true` | `CountToken=false` |
| `GET_MEDIA_TOKEN` | Count image tokens in streaming mode | `true` | - |
| `GET_MEDIA_TOKEN_NOT_STREAM` | Count image tokens in non-streaming mode | `false` | - |

## Multimodal Resource Storage Configuration

Used for automatic URL storage when text-only models call MCP image/video understanding tools.

| Environment Variable | Description | Default Value |
| --- | --- | --- |
| `MAX_IMAGE_UPLOAD_MB` | Maximum size of a single image (MB); `0` means unlimited | `64` |
| `MAX_VIDEO_UPLOAD_MB` | Maximum size of a single video (MB); `0` means unlimited | `128` |
| `STORED_IMAGE_POOL_MB` | Maximum image storage pool size (MB); oldest items are deleted automatically when exceeded | `512` |
| `STORED_VIDEO_POOL_MB` | Maximum video storage pool size (MB); oldest items are deleted automatically when exceeded | `1024` |

## Specific Model Configuration

| Environment Variable | Description | Default Value | Example |
| --- | --- | --- | --- |
| `AZURE_DEFAULT_API_VERSION` | Default API version for the Azure channel | `2025-04-01-preview` | - |
| `COHERE_SAFETY_SETTING` | Cohere model safety setting | `NONE` | - |
| `GEMINI_VISION_MAX_IMAGE_NUM` | Maximum number of images for Gemini models | `16` | - |
| `DIFY_DEBUG` | Output workflow and node information for the Dify channel | `true` | - |

## TLS and Trusted Redirects

| Environment Variable | Description | Default Value | Example |
| --- | --- | --- | --- |
| `TLS_INSECURE_SKIP_VERIFY` | Skip TLS certificate verification for self-signed certificates | `false` | - |
| `TRUSTED_REDIRECT_DOMAINS` | Comma-separated list of trusted domains, with subdomain matching support, used to validate payment callback URLs | empty | `TRUSTED_REDIRECT_DOMAINS=example.com` |

## Metadata Sync

| Environment Variable | Description | Default Value | Example |
| --- | --- | --- | --- |
| `SYNC_UPSTREAM_BASE` | Upstream address for model/vendor metadata | `https://basellm.github.io/llm-metadata` | - |
| `SYNC_HTTP_TIMEOUT_SECONDS` | Sync HTTP timeout (seconds) | `10` | - |
| `SYNC_HTTP_RETRY` | Sync retry count | `3` | - |
| `SYNC_HTTP_MAX_MB` | Maximum sync response body size (MB) | `10` | - |
| `SYNC_HTTP_MAX_IDLE_CONNS` | Maximum idle connections for the model metadata sync client | `10` | - |
| `SYNC_HTTP_IDLE_CONN_TIMEOUT` | Idle connection retention time for the model metadata sync client (seconds) | `30` | - |

## Analytics

| Environment Variable | Description | Default Value | Example |
| --- | --- | --- | --- |
| `UMAMI_WEBSITE_ID` | Umami website ID; enables the Umami analytics script when set | - | - |
| `UMAMI_SCRIPT_URL` | Umami script URL | `https://analytics.umami.is/script.js` | - |
| `GOOGLE_ANALYTICS_ID` | Google Analytics 4 website ID | - | - |

## Other Configuration

| Environment Variable | Description | Default Value | Example |
| --- | --- | --- | --- |
| `ERROR_LOG_ENABLED` | Record and display error logs in the frontend | `false` | `ERROR_LOG_ENABLED=true` |

## Debugging and Profiling

| Environment Variable | Description | Default Value | Example |
| --- | --- | --- | --- |
| `ENABLE_PPROF` | Enable the pprof profiling endpoint (port 8005) | `false` | - |
| `DEBUG` | Enable debug mode with verbose logs and GORM debug queries | `false` | - |
| `PYROSCOPE_URL` | Pyroscope continuous profiling server URL; disabled when unset | - | `PYROSCOPE_URL=http://localhost:4040` |
| `PYROSCOPE_APP_NAME` | Pyroscope application name | `nookmux` | - |
| `PYROSCOPE_BASIC_AUTH_USER` / `PYROSCOPE_BASIC_AUTH_PASSWORD` | Pyroscope Basic Auth credentials | - | - |
| `PYROSCOPE_MUTEX_RATE` / `PYROSCOPE_BLOCK_RATE` | Pyroscope sampling rate | `5` | - |
| `HOSTNAME` | Hostname reported to the profiler | - | - |

## LinuxDo Related (Usually No Changes Needed)

| Environment Variable | Description | Default Value |
| --- | --- | --- |
| `LINUX_DO_TOKEN_ENDPOINT` | LinuxDo token endpoint | `https://connect.linux.do/oauth2/token` |
| `LINUX_DO_USER_ENDPOINT` | LinuxDo user endpoint | `https://connect.linux.do/api/user` |

## Frontend Build-Time Configuration

| Environment Variable | Description | Default Value | Example |
| --- | --- | --- | --- |
| `VITE_REACT_APP_SERVER_URL` | Backend address used by the frontend during build and development | Dev proxy points to `http://localhost:3000` | `VITE_REACT_APP_SERVER_URL=https://api.example.com` |

<Callout type="info">
  `VITE_REACT_APP_SERVER_URL` only affects the frontend dev server proxy target. In production, the frontend uses same-origin requests by default, so this variable is not needed.
</Callout>

## Deprecated / Removed Environment Variables

The following variables are no longer read by the current code, but they are kept here to avoid misconfiguration:

| Environment Variable | Status |
| --- | --- |
| `UPDATE_TASK` | Removed; async task updates are controlled by the system scheduler |
| `ENABLE_METRIC`, `METRIC_QUEUE_SIZE`, `METRIC_SUCCESS_RATE_THRESHOLD` | Removed; channel auto-disable and related features are controlled by monitoring settings in System Settings |
| `TEST_PROMPT` | Removed; test prompts are controlled by System Settings |
| `TASK_PRICE_PATCH` | Removed; use the multiplier settings in System Settings for task pricing |
| `REDIS_MASTER_NAME`, `REDIS_CONN_POOL_SIZE`, `REDIS_PASSWORD` | Removed; Redis configuration is now controlled through the `REDIS_CONN_STRING` connection string and variables such as `REDIS_POOL_SIZE` |
| `GEMINI_MODEL_MAP`, `GEMINI_SAFETY_SETTING` | Deprecated; configure them in System Settings -> Model Settings |

## Multi-Machine Deployment Example

### Master Node Configuration

```bash
# Database configuration - use an external database
SQL_DSN=postgres://user:password@db-server:5432/nookmux

# Security configuration
SESSION_SECRET=your_unique_session_secret
CRYPTO_SECRET=your_unique_crypto_secret

# Redis cache configuration
REDIS_CONN_STRING=redis://:password@redis-server:6379
```

### Slave Node Configuration

```bash
# Database configuration - use the same remote database
SQL_DSN=postgres://user:password@db-server:5432/nookmux

# Security configuration - use the same secrets as the master node
SESSION_SECRET=your_unique_session_secret
CRYPTO_SECRET=your_unique_crypto_secret

# Redis cache configuration - use the same Redis instance as the master node
REDIS_CONN_STRING=redis://:password@redis-server:6379

# Node type setting
NODE_TYPE=slave

# Frontend base URL, only effective on slave nodes
FRONTEND_BASE_URL=https://your-domain.com
```

<Callout type="info" title="Complete Cluster Configuration">
  For the complete cluster deployment configuration, architecture notes, and best practices, see the [Cluster Deployment Guide](/en/docs/installation/deployment-methods/cluster-deployment).
</Callout>
