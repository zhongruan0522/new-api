# 渠道

> 复核日期：2026-08-24。复核方式：对照 `参考项目/newapi-ffp` 中各提交的实际 diff，逐项检查本仓库
> 对应代码路径。结论：**所有三级标题功能点均未在本项目实现**，与差异文档描述一致，无需从清单移除条目。


## 任务一

~~~
/goal 【/workspace/new-api/docs/PRD/20260825-渠道.md】

请你完成任务【### 渠道重试与限额

新增渠道级重试机制，支持设置每个渠道的每日成功次数限额。

- 3f0f62fb-新增渠道级重试
- 41b1454c-支持设置每一个渠道的每日成功次数限额

> 复核结论：**未实现**。本项目仅有全局重试 `common.RetryTimes`（`internal/common/constants.go`、
> 系统设置通用页），`internal/store/channel/channel.go` 的 Channel 模型无 `retry_times` 字段，
> 重试循环（`internal/httpapi/controller/relay/relay.go`）只读全局值，无渠道级
> EffectiveRetryTimes 覆盖；亦无 `daily_success_limit`/`daily_success_count`/`daily_success_date`
> 字段及 Reserve/Release 限额逻辑，前端渠道表单无对应字段。】

不得丢失任何业务逻辑，不得使用模拟数据，提交之前请确保涉及到的AGENTS.md对文件link为最新，完成后commit，请勿因为PRD号称安全就放松，而不注意细节问题

1. 开工之前请对实际情况进行调研，PRD文档也有局限性且编写时间过了一段时间，这个期间修复了挺多UG等内容，作为执行者你需要做好充足的调研再开工。
2. 如果代码已完成请你进行审查，审查的时候请勿过度相信执行者的准确性，在大部分情况下，他所谓的完全完成都是只完成了70%、甚至更低，请你仔细审查，善用子代理分工，也不要盲目相信【执行记录】
3. 禁止在本地跑编译，由于内存限制，请你pull到云端，确保CI过了，确保CI过了,每完成一个任务就需要commit一次
~~~


### 渠道重试与限额

新增渠道级重试机制，支持设置每个渠道的每日成功次数限额。

- 3f0f62fb-新增渠道级重试
- 41b1454c-支持设置每一个渠道的每日成功次数限额

> 复核结论：**未实现**。本项目仅有全局重试 `common.RetryTimes`（`internal/common/constants.go`、
> 系统设置通用页），`internal/store/channel/channel.go` 的 Channel 模型无 `retry_times` 字段，
> 重试循环（`internal/httpapi/controller/relay/relay.go`）只读全局值，无渠道级
> EffectiveRetryTimes 覆盖；亦无 `daily_success_limit`/`daily_success_count`/`daily_success_date`
> 字段及 Reserve/Release 限额逻辑，前端渠道表单无对应字段。

## 任务二

~~~
/goal 【/workspace/new-api/docs/PRD/20260825-渠道.md】

请你完成任务【### DeepSeek 渠道额度查询

- 75eacc6f-修复 DeepSeek 渠道余额查询的 USD 支持

> 复核结论：**未实现**。本项目 `updateChannelDeepSeekBalance`
> （`internal/httpapi/controller/channel/channel_billing.go`）仍是旧逻辑：仅在
> `balance_infos` 中查找 CNY，找不到即报 "currency CNY not found"。无
> `parseDeepSeekBalanceUSD`（USD 优先）、无 CNY→USD 换算（参考实现用 `operation_setting.Price`
> 作汇率）、无 `normalizeDeepSeekCurrency`，也无对应单测。


### OpenCode Zen / Go 渠道适配

新增 OpenCode Zen 与 OpenCode Go 两个渠道的完整支持（模型路由、请求处理、adaptor/constants 与测试），扩展 OpenCode 生态的集成能力。

- 3a8819f3-feat: 添加OpenCode Zen通道支持
- d18964df-feat: 添加OpenCode Go渠道

> 复核结论：**未实现**。`internal/relay/channel/` 下无 `opencode/` 包，
> `internal/domain/channel/constant/` 无 OpenCode 渠道/端点/API 类型常量，relay 路由与
> openaicompat policy 均无相关分支。全仓 `opencode` 仅出现在 README/docs 与 CI 注释中，
> 与渠道适配无关。】

不得丢失任何业务逻辑，不得使用模拟数据，提交之前请确保涉及到的AGENTS.md对文件link为最新，完成后commit，请勿因为PRD号称安全就放松，而不注意细节问题

1. 开工之前请对实际情况进行调研，PRD文档也有局限性且编写时间过了一段时间，这个期间修复了挺多UG等内容，作为执行者你需要做好充足的调研再开工。
2. 如果代码已完成请你进行审查，审查的时候请勿过度相信执行者的准确性，在大部分情况下，他所谓的完全完成都是只完成了70%、甚至更低，请你仔细审查，善用子代理分工，也不要盲目相信【执行记录】
3. 禁止在本地跑编译，由于内存限制，请你pull到云端，确保CI过了，确保CI过了,每完成一个任务就需要commit一次
~~~

### DeepSeek 渠道额度查询

- 75eacc6f-修复 DeepSeek 渠道余额查询的 USD 支持

> 复核结论：**未实现**。本项目 `updateChannelDeepSeekBalance`
> （`internal/httpapi/controller/channel/channel_billing.go`）仍是旧逻辑：仅在
> `balance_infos` 中查找 CNY，找不到即报 "currency CNY not found"。无
> `parseDeepSeekBalanceUSD`（USD 优先）、无 CNY→USD 换算（参考实现用 `operation_setting.Price`
> 作汇率）、无 `normalizeDeepSeekCurrency`，也无对应单测。


### OpenCode Zen / Go 渠道适配

新增 OpenCode Zen 与 OpenCode Go 两个渠道的完整支持（模型路由、请求处理、adaptor/constants 与测试），扩展 OpenCode 生态的集成能力。

- 3a8819f3-feat: 添加OpenCode Zen通道支持
- d18964df-feat: 添加OpenCode Go渠道

> 复核结论：**未实现**。`internal/relay/channel/` 下无 `opencode/` 包，
> `internal/domain/channel/constant/` 无 OpenCode 渠道/端点/API 类型常量，relay 路由与
> openaicompat policy 均无相关分支。全仓 `opencode` 仅出现在 README/docs 与 CI 注释中，
> 与渠道适配无关。

## 任务三

~~~
/goal 【/workspace/new-api/docs/PRD/20260825-渠道.md】

请你完成任务【### DeepSeek 渠道额度查询

- 75eacc6f-修复 DeepSeek 渠道余额查询的 USD 支持

> 复核结论：同任务二，**未实现**。

### OpenAI / Claude 付费层级识别

渠道测试支持显示 OpenAI key 与 Anthropic Claude 的付费层级，提升渠道管理透明度。

- 52d0c3ed-支持显示openai key付费层级
- 6ef00d14-支持显示Anthropic Claude付费层级

> 复核结论：**未实现**。渠道测试 `internal/httpapi/controller/channel/channel_test_handler.go`
> 无 rate-limit 响应头解析（OpenAI `x-ratelimit-*` / Anthropic `anthropic-ratelimit-*`）、
> 无 `isOfficialOpenAIChannel` 判断、无 tier 推断，测试结果结构不含 rateLimit 字段；前端
> `channel-test-dialog.tsx` 无层级/限额展示。（仓库中搜到的 "tier" 均为计费分层 pricing，
> 与本功能无关。）】

不得丢失任何业务逻辑，不得使用模拟数据，提交之前请确保涉及到的AGENTS.md对文件link为最新，完成后commit，请勿因为PRD号称安全就放松，而不注意细节问题

1. 开工之前请对实际情况进行调研，PRD文档也有局限性且编写时间过了一段时间，这个期间修复了挺多UG等内容，作为执行者你需要做好充足的调研再开工。
2. 如果代码已完成请你进行审查，审查的时候请勿过度相信执行者的准确性，在大部分情况下，他所谓的完全完成都是只完成了70%、甚至更低，请你仔细审查，善用子代理分工，也不要盲目相信【执行记录】
3. 禁止在本地跑编译，由于内存限制，请你pull到云端，确保CI过了，确保CI过了,每完成一个任务就需要commit一次
~~~

### DeepSeek 渠道额度查询

- 75eacc6f-修复 DeepSeek 渠道余额查询的 USD 支持

> 复核结论：同任务二，**未实现**。

### OpenAI / Claude 付费层级识别

渠道测试支持显示 OpenAI key 与 Anthropic Claude 的付费层级，提升渠道管理透明度。

- 52d0c3ed-支持显示openai key付费层级
- 6ef00d14-支持显示Anthropic Claude付费层级

> 复核结论：**未实现**。渠道测试 `internal/httpapi/controller/channel/channel_test_handler.go`
> 无 rate-limit 响应头解析（OpenAI `x-ratelimit-*` / Anthropic `anthropic-ratelimit-*`）、
> 无 `isOfficialOpenAIChannel` 判断、无 tier 推断，测试结果结构不含 rateLimit 字段；前端
> `channel-test-dialog.tsx` 无层级/限额展示。（仓库中搜到的 "tier" 均为计费分层 pricing，
> 与本功能无关。）

## 任务四

~~~
/goal 【/workspace/new-api/docs/PRD/20260825-渠道.md】

请你完成任务【### Claude 与 AWS 优化

支持新版 Claude 模型；AWS 客户端增加推理配置 ID 验证、重构客户端创建流程并新增实时集成测试。

- 12e18193-feat: Add support for new Claude model version and enhance AWS client

> 复核结论：**基本未实现**。逐项核对（`internal/relay/channel/aws/`）：
> - `claude-opus-4-8` 模型常量：无（`constants.go` 最新为 opus-4-6）；
> - 客户端重构：无独立 `client.go`，凭据解析内联在 `relay_aws.go` 的 `newAwsClient`，无区域
>   正则校验（`awsRegionPattern`）；
> - inference profile ID 验证：无；
> - 从 AWS 拉取 Claude 模型与推理配置（`models.go` + bedrock 控制面 `ListFoundationModels`
>   / `ListInferenceProfiles`）：无（仅使用 `bedrockruntime`）；
> - DTO 指针化：部分完成（`Temperature` 已为 `*float64`；`MaxTokens uint`、`TopP float64`、
>   `TopK int` 仍为值类型，显式零值会被 `omitempty` 丢弃）；
> - `client_test.go` / `models_test.go` / `live_integration_test.go`：均无。
> 注：AWS API key/AKSK 双模式（`AwsKeyType`）本项目已具备，但该能力来自参考仓库更早的
> 59caea8f 提交，不属于本条目范围。

### 不兼容的请求头

移除不支持的 computer_call_output 参数与不兼容的 x-anthropic-billing-header 请求头，修复 API 兼容性问题。

- e41e75ad-移除不支持的computer_call_output参数
- 35aacb3f-去你妈的x-anthropic-billing-header

> 复核结论：**未实现**。
> - e41e75ad：本项目无 xai 渠道包，无 `codex_compat.go` / `normalizeXAIResponsesInput`
>   （Responses input 中 `computer_call_output` 等不支持项的归一化剔除）；
> - 35aacb3f：`internal/config/model/claude.go` 的 `ClaudeSettings` 无
>   `RemoveClaudeCodeBillingHeaderEnabled` 开关，`WriteHeaders` 不删除该 header，header
>   passthrough 与参数覆写的 header copy 路径也无跳过逻辑；代码中无
>   `x-anthropic-billing-header` 字符串（仅存在于 docs）。】

不得丢失任何业务逻辑，不得使用模拟数据，提交之前请确保涉及到的AGENTS.md对文件link为最新，完成后commit，请勿因为PRD号称安全就放松，而不注意细节问题

1. 开工之前请对实际情况进行调研，PRD文档也有局限性且编写时间过了一段时间，这个期间修复了挺多UG等内容，作为执行者你需要做好充足的调研再开工。
2. 如果代码已完成请你进行审查，审查的时候请勿过度相信执行者的准确性，在大部分情况下，他所谓的完全完成都是只完成了70%、甚至更低，请你仔细审查，善用子代理分工，也不要盲目相信【执行记录】
3. 禁止在本地跑编译，由于内存限制，请你pull到云端，确保CI过了，确保CI过了,每完成一个任务就需要commit一次
~~~

### Claude 与 AWS 优化

支持新版 Claude 模型；AWS 客户端增加推理配置 ID 验证、重构客户端创建流程并新增实时集成测试。

- 12e18193-feat: Add support for new Claude model version and enhance AWS client

> 复核结论：**基本未实现**。逐项核对（`internal/relay/channel/aws/`）：
> - `claude-opus-4-8` 模型常量：无（`constants.go` 最新为 opus-4-6）；
> - 客户端重构：无独立 `client.go`，凭据解析内联在 `relay_aws.go` 的 `newAwsClient`，无区域
>   正则校验（`awsRegionPattern`）；
> - inference profile ID 验证：无；
> - 从 AWS 拉取 Claude 模型与推理配置（`models.go` + bedrock 控制面 `ListFoundationModels`
>   / `ListInferenceProfiles`）：无（仅使用 `bedrockruntime`）；
> - DTO 指针化：部分完成（`Temperature` 已为 `*float64`；`MaxTokens uint`、`TopP float64`、
>   `TopK int` 仍为值类型，显式零值会被 `omitempty` 丢弃）；
> - `client_test.go` / `models_test.go` / `live_integration_test.go`：均无。
> 注：AWS API key/AKSK 双模式（`AwsKeyType`）本项目已具备，但该能力来自参考仓库更早的
> 59caea8f 提交，不属于本条目范围。

### 不兼容的请求头

移除不支持的 computer_call_output 参数与不兼容的 x-anthropic-billing-header 请求头，修复 API 兼容性问题。

- e41e75ad-移除不支持的computer_call_output参数
- 35aacb3f-去你妈的x-anthropic-billing-header

> 复核结论：**未实现**。
> - e41e75ad：本项目无 xai 渠道包，无 `codex_compat.go` / `normalizeXAIResponsesInput`
>   （Responses input 中 `computer_call_output` 等不支持项的归一化剔除）；
> - 35aacb3f：`internal/config/model/claude.go` 的 `ClaudeSettings` 无
>   `RemoveClaudeCodeBillingHeaderEnabled` 开关，`WriteHeaders` 不删除该 header，header
>   passthrough 与参数覆写的 header copy 路径也无跳过逻辑；代码中无
>   `x-anthropic-billing-header` 字符串（仅存在于 docs）。

## 任务五

~~~
/goal 【/workspace/new-api/docs/PRD/20260825-渠道.md】

请你完成任务【### Vertex 渠道优化

补全 Vertex TTS/STT 功能适配与测试（STT 请求去除 x-goog-user-project 头），修复模型列表获取配置（含完整测试），pageSize 遵循官方限制 300。

- cc4dd826 / e3920f2e / 13815f58 / 17d7a04a-补全vertex tts stt测试文件
- 3f775810-vertex stt去除x-goog-user-project
- a2811c6b-修复 vertex 渠道的模型列表获取配置
- f3ade239-Vertex 模型列表的 pageSize 官方限制300

> 复核结论：**未实现**。逐项核对：
> - TTS/STT 渠道（参考实现为新增独立 `relay/channel/gcp/` 渠道类型）：本项目无 `gcp/` 包、
>   无对应 ChannelType/APIType/EndpointType 常量；现有 vertex 渠道 `ConvertAudioRequest`
>   直接返回 "not implemented"（`internal/relay/channel/vertex/adaptor.go`），相关测试自然缺失；
> - STT 去除 `x-goog-user-project`：该改动位于参考项目的 gcp 渠道 STT 路径，随渠道缺失未
>   实现。本项目 vertex 主路径仍设置该 header（`adaptor.go` 的 `SetupRequestHeader`），与
>   参考项目 vertex 主渠道行为一致，属正常保留；
> - 模型列表获取：`FetchModels`（`internal/httpapi/controller/channel/channel.go`）对
>   vertex 渠道无特殊分支（仅 Ollama/Gemini 有），走默认 `/v1/models`，无法用 service
>   account 拉取 `publishers/google/models`；无 `model_list.go` / `FetchGoogleModels` /
>   `BuildGoogleModelsListURL` 及测试；
> - pageSize 300：无模型列表 URL 构造逻辑，无 pageSize 参数。
> 注：本项目 `internal/relay/channel/vertex/url_builder.go` 已有 gateway 前缀/region 处理等
> 同名文件，属本项目自有实现，与参考提交的 url_builder 内容不同，不构成该条目的部分实现。】

不得丢失任何业务逻辑，不得使用模拟数据，提交之前请确保涉及到的AGENTS.md对文件link为最新，完成后commit，请勿因为PRD号称安全就放松，而不注意细节问题

1. 开工之前请对实际情况进行调研，PRD文档也有局限性且编写时间过了一段时间，这个期间修复了挺多UG等内容，作为执行者你需要做好充足的调研再开工。
2. 如果代码已完成请你进行审查，审查的时候请勿过度相信执行者的准确性，在大部分情况下，他所谓的完全完成都是只完成了70%、甚至更低，请你仔细审查，善用子代理分工，也不要盲目相信【执行记录】
3. 禁止在本地跑编译，由于内存限制，请你pull到云端，确保CI过了，确保CI过了,每完成一个任务就需要commit一次
~~~

### Vertex 渠道优化

补全 Vertex TTS/STT 功能适配与测试（STT 请求去除 x-goog-user-project 头），修复模型列表获取配置（含完整测试），pageSize 遵循官方限制 300。

- cc4dd826 / e3920f2e / 13815f58 / 17d7a04a-补全vertex tts stt测试文件
- 3f775810-vertex stt去除x-goog-user-project
- a2811c6b-修复 vertex 渠道的模型列表获取配置
- f3ade239-Vertex 模型列表的 pageSize 官方限制300

> 复核结论：**未实现**。逐项核对：
> - TTS/STT 渠道（参考实现为新增独立 `relay/channel/gcp/` 渠道类型）：本项目无 `gcp/` 包、
>   无对应 ChannelType/APIType/EndpointType 常量；现有 vertex 渠道 `ConvertAudioRequest`
>   直接返回 "not implemented"（`internal/relay/channel/vertex/adaptor.go`），相关测试自然缺失；
> - STT 去除 `x-goog-user-project`：该改动位于参考项目的 gcp 渠道 STT 路径，随渠道缺失未
>   实现。本项目 vertex 主路径仍设置该 header（`adaptor.go` 的 `SetupRequestHeader`），与
>   参考项目 vertex 主渠道行为一致，属正常保留；
> - 模型列表获取：`FetchModels`（`internal/httpapi/controller/channel/channel.go`）对
>   vertex 渠道无特殊分支（仅 Ollama/Gemini 有），走默认 `/v1/models`，无法用 service
>   account 拉取 `publishers/google/models`；无 `model_list.go` / `FetchGoogleModels` /
>   `BuildGoogleModelsListURL` 及测试；
> - pageSize 300：无模型列表 URL 构造逻辑，无 pageSize 参数。
> 注：本项目 `internal/relay/channel/vertex/url_builder.go` 已有 gateway 前缀/region 处理等
> 同名文件，属本项目自有实现，与参考提交的 url_builder 内容不同，不构成该条目的部分实现。