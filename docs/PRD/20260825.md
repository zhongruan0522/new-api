# FuturePPO/new-api 提交历史（按功能模块归类 · 简写版）

## 项目概况

- **仓库**: [https://github.com/futureppo/new-api](https://github.com/futureppo/new-api)
- **来源**: 基于《futureppo-commits-analysis.md》与《futureppo-commits-by-feature.md》整理
- **格式**: 每个功能点列出所属提交，格式为 `commit hash-提交标题`（简写版，不含原时间线编号）

---

## 渠道

### 渠道重试与限额

新增渠道级重试机制，支持设置每个渠道的每日成功次数限额。

- 3f0f62fb-新增渠道级重试
- 41b1454c-支持设置每一个渠道的每日成功次数限额

### 火山引擎渠道适配

- 62ddf4d5-修复火山渠道

### DeepSeek 渠道额度查询

- 75eacc6f-修复 DeepSeek 渠道余额查询的 USD 支持

### Claude 与 AWS 优化

支持新版 Claude 模型；AWS 客户端增加推理配置 ID 验证、重构客户端创建流程并新增实时集成测试。

- 12e18193-feat: Add support for new Claude model version and enhance AWS client

### OpenCode Zen / Go 渠道适配

新增 OpenCode Zen 与 OpenCode Go 两个渠道的完整支持（模型路由、请求处理、adaptor/constants 与测试），扩展 OpenCode 生态的集成能力。

- 3a8819f3-feat: 添加OpenCode Zen通道支持
- d18964df-feat: 添加OpenCode Go渠道

### OpenAI / Claude 付费层级识别

渠道测试支持显示 OpenAI key 与 Anthropic Claude 的付费层级，提升渠道管理透明度。

- 52d0c3ed-支持显示openai key付费层级
- 6ef00d14-支持显示Anthropic Claude付费层级

### 不兼容的请求头

移除不支持的 computer_call_output 参数与不兼容的 x-anthropic-billing-header 请求头，修复 API 兼容性问题。

- e41e75ad-移除不支持的computer_call_output参数
- 35aacb3f-去你妈的x-anthropic-billing-header

### Vertex 渠道优化

补全 Vertex TTS/STT 功能适配与测试（STT 请求去除 x-goog-user-project 头），修复模型列表获取配置（含完整测试），pageSize 遵循官方限制 300。

- cc4dd826 / e3920f2e / 13815f58 / 17d7a04a-补全vertex tts stt测试文件
- 3f775810-vertex stt去除x-goog-user-project
- a2811c6b-修复 vertex 渠道的模型列表获取配置
- f3ade239-Vertex 模型列表的 pageSize 官方限制300

---

## 使用日志

### 使用日志完善（兑换码 IP 记录等）

使用日志新增 token 输出速度、RPM 统计、IP 筛选等展示能力，后端日志新增 user_agent 字段，签到与兑换码请求记录 IP 与 User-Agent，实现用户行为追踪。

- 8c4a88b9-feat(ui): 使用日志添加token输出速度展示
- dc7e9452-使用日志支持筛选ip
- c2212843-修复 花费时间 显示问题
- 7f252753-使用日志 RPM 展示修改
- c2ff6ab3-后端 Log 新增 user_agent 字段
- 001d8031-普通日志现在可以记录签到以及兑换码的请求 IP 和 User-Agent

### 使用日志封禁用户

支持在使用日志部分快速禁用用户，提升管理员操作效率。

- ef19fb40-支持在使用日志部分快速禁用用户

### 邮件发送日志

邮件发送日志与 IP 追踪，管理员可见邮件日志。

- bcbc8860-邮件发送日志和 IP 追踪
- e61d072b-管理员可见的邮件日志

---

## 系统设置

### 批量封禁与测活封禁

支持批量封禁与自动封禁批量测活（可配置参数），并持续修复封禁问题、优化封禁逻辑和错误处理。

- e4b24491-支持批量封禁
- 8c7dfe06-支持自动封禁批量测活，可配置参数
- 10da1c0a-修复测活封禁机制
- 3f672407 / 2c53b98b-修复封禁问题
- 9a6f5c0f-风控中心优化封禁

### 注册邀请码

用户注册要求邀请码，集成 OAuth 和微信注册流程，含注册码配置增强、全面测试与多语言支持。

- fe707be9-feat: add invite code requirement for user registration

### 全局并发限制与渠道并发

实现全局并发请求限制，渠道级 RPM 保护（内存和 Redis 两种追踪方式、基于上游响应的动态负载降低、多语言 RPM 保护设置与全面测试）。

- dbb5dab2-支持限制并发
- 92754662-feat: implement RPM protection for channels

### GitHub 注册年限限制

风控功能：封禁/限制 GitHub 账号注册时间，防止新注册账号滥用。

- a952a8f0-支持封禁/限制github注册时间

### 日志分析

- 34a7c34d-移除日志分析

### 邮箱白名单 / 黑名单通配符

- 77a9ff4f-支持通配邮箱后缀

---

## 用户管理

### 连锁封禁（邀请连坐）

基于邀请关系的连锁封禁机制：新增服务层处理邀请关系封禁与前端 UI；后续支持多层（递归）查询、禁用邀请码用户处理，并默认封禁全部邀请关联账号。

- 8828ba51-支持连锁封禁
- 4c73bcde-添加禁用邀请码用户的处理逻辑
- f63c509a-支持邀请连坐多层查询
- f5064163-默认封禁全部邀请关联账号

---

## GitHub

### CI / Docker 构建优化

创建 fork 专属 GHCR 工作流，支持多架构镜像发布，后续重构 Docker 工作流（多架构构建、元数据准备流程优化）。

- 59b5f619-Create docker-ghcr-fork.yml
- 67644a75-ci: publish multi-arch GHCR images
- 972272fc-feat: 重构Docker工作流

---

## 安全性

### 空 AccessToken 拒绝访问

- 554e2b9a-fix(auth): reject empty access tokens

