---
title: 关于
---

顶部导航的 **关于** 页面由 **系统设置 → 站点与品牌 → 系统信息** 中的 `关于` 字段控制。

- 留空：展示项目默认说明与 `NookMux/NookMux` 仓库链接。
- 填写 URL：页面以内嵌 iframe 打开该地址。
- 填写 HTML：按 HTML 内容渲染。
- 其他文本：按 Markdown 内容渲染。

相关实现：`web/src/features/about/index.tsx`。
