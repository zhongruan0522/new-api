---
title: 安全与限制
---

旧文档中的“速率限制设置”现在位于 **系统设置 → 安全与限制 → 速率限制**。

可配置项包括：

- 是否启用模型请求速率限制。
- 每个限制周期内最大请求数。
- 每个限制周期内最大成功请求数。
- 限制周期（分钟）。
- 基于用户分组的速率限制覆盖。

分组速率限制示例：

```json
{
  "default": [200, 100],
  "vip": [0, 1000]
}
```

同一分组下的具体含义以当前页面字段说明为准。安全与限制分组还包含敏感词、SSRF 防护和审计日志配置。
