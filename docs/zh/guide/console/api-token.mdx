---
title: API 密钥
---

API 密钥页面入口为 `/keys`，用于创建、查看、编辑、重置和删除用户 API 密钥。

常见能力：

- 设置密钥名称、额度、过期时间和是否启用。
- 选择密钥可用分组与可调用模型。
- 配置 auto 分组、跨分组重试等调用策略（具体字段以当前页面为准）。
- 批量操作与复制调用配置。

管理员可通过 **系统设置 → 计费与支付 → 分组定价** 配置用户可选分组、自动分组和分组倍率。
