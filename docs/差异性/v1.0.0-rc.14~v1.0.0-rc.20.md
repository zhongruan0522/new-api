# 差异性文档

> 时间: 2026-08-19
> 对比上游 NewAPI 现有版本: `v1.0.0-rc.13`（2026-06-19）→ `v1.0.0-rc.20`（2026-07-07，含 `v1.0.0-rc.19-i18nfix.1/2` 两个补丁标签）
> 共计 **134** 个 Commit（130 个非 merge）为差异需求
>
> 本文档只收录上游对 **本仓库仍保留功能** 的修复 / 新增 / 完善。fork 已删除的渠道（kling/jimeng/vidu/sora/coze/dify/xai/volcengine/baidu/ali/xunfei/tencent/360/perplexity/cohere/cloudflare/mistral/jina/midjourney/suno/xinference/replicate/codex/stepfun/ai360/waffo 等）、已删除功能（electron/订阅/task 异步任务框架/video_proxy/web classic 老前端等）以及 fork 已吸收的提交均不在此列（完整判定见文末附录）。
>
> **fork 布局提示**：本仓库已重构为 `internal/` 布局（模块 `github.com/NookMux/NookMux`）。上游路径对应关系：`common/`→`internal/common/`、`setting/`→`internal/config/`、`relay/`→`internal/relay/`、`logger/`→`internal/infra/log/`、`web/default/src/`→`web/src/`。下文"涉及文件"同时标注上游路径与 fork 对应路径。
>
> 核验方法：7 批子代理逐条分析（覆盖全部 130 个非 merge 提交）+ 主代理对 P0 级结论逐一比对仓库代码复核。

---

## 安全性

### P0

#### `dfc0d6324` 用户设置更新竞态可回滚已消费配额（安全通告合并修复，fork 残余三个子项）

上游修复内容：①`PUT /api/user/self` 任意 JSON 反序列化为 `model.User` 全量 Updates（mass assignment 可改 quota）；②设置更新走全行 Update，读-改-写竞态内用过期快照覆盖 `quota`/`used_quota`/`request_count`；③`updateUserCache` 把快照 Quota 写回缓存；④`PUT /self` 缺 `CriticalRateLimit`。

涉及文件：
1. `model/user.go` → `internal/model/user.go`（`Update` 无 `Omit("quota","used_quota","request_count")`）
2. `model/user_cache.go` → `internal/model/user_cache.go:66-75`（`updateUserCache` 经 RedisHSetObj 全量写含 Quota 的 UserBase）
3. `controller/user.go` → `internal/controller/user.go`（`UpdateUserSetting` 取全量快照后调 `user.Update(false)`）
4. `router/api-router.go` → `internal/router/api_router.go:76`（`PUT /user/self` 无 CriticalRateLimit）

原因（fork 现状证据）：
1. 子项①fork 已免疫：`UpdateSelf` 用 `cleanUser` 白名单（`internal/controller/user.go:596-601` 仅 Id/Username/Password/DisplayName）。
2. 子项②③仍成立：`internal/controller/user.go` 的 `UpdateUserSetting` 先 `GetUserById` 取全量快照，改 setting 后调 `user.Update(false)`；`internal/model/user.go:421-437` 的 `Update` 用 `DB.Model(user).Updates(newUser)` 把快照全部非零字段回写且不 Omit 计费三字段——并发消费期间反复 `PUT /user/setting`，快照中非零的旧 Quota 会覆盖当前值，**用户可借此恢复已耗额度**（免费刷配额）。
3. 子项④仍成立：`PUT /user/self`（`internal/router/api_router.go:76`）与 `PUT /user/setting`（`:96`）均未挂 `CriticalRateLimit`（同组 `register`/`reset_password`/`topup` 均有挂），竞态可高频执行。

应当表现为：
1. 新增只更新 setting 列的 `UpdateUserSetting(userId, setting)`；`Update`/`Edit` 路径 `Omit("quota","used_quota","request_count")`；`updateUserCache` 不写 Quota（配额统一由原子增量路径维护）；`PUT /user/self` 挂 `CriticalRateLimit`；参照上游补 user_update 回归测试（先证伪再修复）。

---

#### 富文本渲染安全六连 `f4473d963` + `0b48ad86d` + `626dadb55` + `fda817786` + `1f4d8d2b2` + `17465b855`（fork 语境升级为 P0）

上游此系列把自定义 HTML/Markdown 渲染收敛为共享 `HtmlContent`/`RichContent` 组件（DOMPurify 消毒 + Shadow DOM 隔离 + iframe sandbox + 暗色同步 + 应用样式注入）。**fork 完全停留在上游修复之前的状态且更糟：全库无任何 HTML 消毒**。

涉及文件（fork 注入点）：
1. `web/src/features/about/index.tsx:176` —— 裸 `dangerouslySetInnerHTML={{ __html: rawContent }}`
2. `web/src/features/legal/legal-document.tsx:145` —— 同上
3. `web/src/components/layout/components/footer.tsx:232` —— 同上（页脚 HTML）
4. `web/src/components/ui/markdown.tsx` —— react-markdown + remark-gfm + `rehypeRaw` **且无 sanitize**（首页自定义内容/公告经此渲染）
5. `web/src/features/home/index.tsx:48` —— 自定义首页 iframe 无 `sandbox` 属性
6. `web/package.json:94` —— `dompurify` 仅作为 overrides 钉在 3.4.5，`web/src` 内零调用（死依赖）
7. `web/src/components/ai-elements/code-block.tsx:129` —— 另有一处 `dangerouslySetInnerHTML`（Shiki 高亮输出，库生成 HTML 风险较低，随整体方案一并评估）

原因：
1. 管理员配置的关于页/法律文档/页脚/首页内容被原样注入 DOM，`<img onerror>`、内联事件等存储型 XSS 直接可打；内容源虽是管理员，但账号失窃或配置写入漏洞即升级为全站访客执行脚本。
2. 上游六提交是一个机制整体：`0b48ad86d` 建 HTML/Markdown 一致检测渲染、`626dadb55`/`f4473d963` 消毒（DOMPurify）、`fda817786` 隔离渲染（Shadow DOM + 白名单放宽 + iframe harden）、`1f4d8d2b2`/`17465b855` 修隔离后的样式/暗色问题。单独移植前四者之一不完整。

应当表现为：
1. fork 至少立即为前 3 处 `dangerouslySetInnerHTML` 与 Markdown 管线引入 DOMPurify 消毒（依赖已在 overrides 中，直接启用）；完整方案按上游 `html-content.tsx`（inline/isolated 两档）+ `rich-content.tsx` 落地，home/about iframe 补 `sandbox`；`1f4d8d2b2`/`17465b855` 随隔离机制一并移植，否则暗色模式与排版在隔离渲染下失效。
2. 实施时需按 fork 的 i18n 嵌套 key 体系与 `@/components/ui/dialog` 路径适配，不可直接 cherry-pick。

---

### P1

#### `0bf42781d` + `35074345e` dompurify 3.4.5 存在已知 CVE，应升至 ≥3.4.11

涉及文件：
1. `web/package.json`（overrides 中 `"dompurify": "3.4.5"`）
2. `web/bun.lock`

原因：
1. fork 的 dompurify 是真实运行时依赖链一环（`streamdown` → `mermaid` → dompurify），被 overrides 强制钉在 3.4.5；3.4.5 受 CVE-2026-49458 / CVE-2026-49459（XSS bypass，3.4.6 修复）、CVE-2026-49978（3.4.7 修复）影响。
2. 若落地上一条的富文本消毒方案，dompurify 将从传递依赖变为直接使用，升级更加必要。

应当表现为：
1. overrides 钉值升至 ≥3.4.11（与上游 `0bf42781d` 对齐，有条件时跟进更新版本）并同步 bun.lock；`bun install`、`bun run build`、`bun run typecheck` 通过，mermaid 图表渲染回归正常。

---

#### `5fc35e28a` 账户邮箱与密码处理加固（含 `4a64b8707` 回归测试）

涉及文件：
1. `model/user.go` → `internal/model/user.go`（无 `NormalizeEmail`/`EnsureEmailAvailable`/`GetUniqueUserByEmail`/`BindEmailToUser`）
2. `controller/user.go` → `internal/controller/user.go`（Register/UpdateSelf/`checkUpdatePassword`/EmailBind）
3. `controller/misc.go` → `internal/controller/misc.go`（SendEmailVerification/SendPasswordResetEmail/ResetPassword）
4. `controller/oauth.go` → `internal/controller/oauth.go`
5. `i18n/locales/*.yaml` → `internal/i18n/{en,zh}.yaml`

原因（fork 现状证据）：
1. 邮箱无规范化与大小写唯一性：`internal/model/user.go:553-555` `IsEmailAlreadyTaken` 精确 `email = ?` 匹配，`oauth.go:244/290` 直接用原始 `oauthUser.Email`；大小写变体可绕过占用检查产生同邮箱多账号。
2. 密码重置无歧义消解：`ResetUserPasswordByEmail` 用 `Where("email = ?").Update(...)`，同邮箱多账号时全部被重置。
3. 无密码账号可直接设密码：`internal/controller/user.go:625-643` `checkUpdatePassword` 保留修复前逻辑（空密码哈希账号经 UpdateSelf 无需任何原凭据即可设置密码），会话被劫持时可固化访问权限。

应当表现为：
1. `NormalizeEmail`（trim+小写）贯穿注册/OAuth/绑定/重置；占用检查改大小写不敏感并事务内串行化；重置密码只作用于唯一匹配账号、歧义报错；`checkUpdatePassword` 对空密码哈希账号返回"未设置密码"错误、强制走邮箱重置流程；携带上游 user_update 系列测试。

---

#### `0565e6267` 仅 401 视为会话失效，网络瞬断不再误登出

涉及文件：
1. `routes/_authenticated/route.tsx` → `web/src/routes/_authenticated/route.tsx:41-53`

原因：
1. fork 逐字保留修复前代码：`getSelf().catch(() => null)`，且把任何失败（网络错误/超时/5xx）当作 session 失效 `auth.reset()` 并踢到登录页；`getSelf` 还传了 `skipErrorHandler: true`，现有 axios 401 拦截器对它不生效。fork 同为 axios（`lib/api.ts`），`err.response?.status === 401` 判定可直接套用。

应当表现为：
1. catch 中仅 401 返回 `{success:false}`，其余错误返回 null 放行待下次导航重验。

---

## 修复

### 计费 / 协议正确性

#### 【P1】`2f5f6ba84` gpt-5.4/5.5/5.6 补全倍率被硬编码 8 遮蔽（多收费 33%~4 倍）

涉及文件：
1. `setting/ratio_setting/model_ratio.go` → `internal/config/ratio/model_ratio.go:856-859`

原因：
1. fork 的 `getHardcodedCompletionModelRatio` 对所有 `gpt-5` 前缀一律 `return 8, true`（第二参数 true 表示短路），而 `GetCompletionRatio` 命中后不再查 `completionRatioMap`。fork 自己的 `defaultCompletionRatio` 明确写了 `gpt-5.4: 6`、`gpt-5.4-image-2: 1.875`、`gpt-5.5: 6`、`gpt-5.6-*: 6` —— 这些表项全部被硬编码 8 遮蔽成死配置。实际后果：gpt-5.5/5.6 补全按 8 计费而非 6，gpt-5.4-image-2 按 8 而非 1.875；管理员倍率覆盖同样失效。
2. 上游本提交把带点号的 5.5+ 改为 `return 6, false`（解锁、优先查表），正是修复该遮蔽。**改动极小、收益直接，建议最先落地。**

应当表现为：
1. 带 `.` 的 gpt-5.x（至少 5.4/5.5/5.6）不再被硬编码短路，`GetCompletionRatio` 优先返回倍率表（含管理员覆盖）；不带点的 `gpt-5*` 维持 8。

---

#### 【P1】`c9943d37a` 音频时长→token 换算无饱和转换，可被伪造时长打爆配额（max_tokens 部分已吸收）

涉及文件：
1. `service/token_counter.go` → `internal/service/token_counter.go:215/384/395`
2. `relay/channel/openai/audio.go` → `internal/relay/channel/openai/audio.go:107`
3. `relay/helper/price.go` → `internal/relay/helper/price.go:71/137/142/208`

原因：
1. 该提交的 max_tokens 校验部分 fork 已等价吸收（`maxTokensUpperBound = math.MaxInt32/2` 覆盖 OpenAI 文本/Claude/Responses/Gemini）。
2. 未吸收部分：fork 的音频时长→token 换算仍是无饱和 `int()` 裸转换，与上游修复前逐字符相同。时长解析自上传/上游音频文件容器头元数据（`internal/common/audio.go` 按 WAV/MP4 头字段推算），可被伪造成天文数字，导致预扣/结算 quota 溢出 int32 库列或回绕。

应当表现为：
1. 引入等价的饱和转换（int32 上下限 + NaN→0），替换 4 处音频 token 换算及 `price.go` 的 4 处 `int(浮点乘积)` 预扣换算（含按次计费 `price.go:208`）。

---

#### `12603a776` 兑换码服务端状态筛选（清理动作部分已吸收）

涉及文件：
1. `model/redemption.go` → `internal/model/redemption.go`（`SearchRedemptions` 加 status 参数 + "expired" 虚拟状态）
2. `controller/redemption.go` → `internal/controller/redemption.go:41`（透传 query）
3. `web/default/src/features/redemption-codes/` → `web/src/features/redemption-codes/`

原因：
1. 已吸收部分：fork 的 `Redeem` 已拒绝过期码、`DeleteInvalidRedemptions` 已覆盖 used/disabled/过期 enabled 并带审计。
2. 缺口：`SearchRedemptions(keyword, startIdx, num)` 无 status 参数，无法按 enabled/disabled/used/expired 服务端筛选；前端状态过滤仅为当前页客户端列过滤（过期码虽已渲染"已过期"徽章、过滤值含 `expired` 虚拟项，但都只作用于当前页已加载行），无法跨页聚合筛出过期码。

应当表现为：
1. `SearchRedemptions` 支持 status 条件（含 expired 虚拟状态）、keyword 可空；controller 透传；前端搜索栏加状态下拉。错误处理遵循 fork 的 `ApiErrorI18n` 规范。

---

### 可靠性 / 健壮性

#### 【P1】`986d90ae0` 服务优雅关闭（避免重启掐断流式回复与看板缓存丢失）+ `8874d1929` 启动日志延迟

涉及文件：
1. `main.go` → `internal/app/server.go:156`（`Run` 直接 `server.Run`）
2. `model.SaveQuotaDataCache` → `internal/model/usedata.go:148`（fork 已有实现但无退出路径调用）

原因：
1. fork 完全没有优雅关闭：`internal/app/` 与 `cmd/` 下 grep `signal.Notify|Shutdown|SIGTERM` 无结果，重启即掐断所有进行中的流式回复；看板缓存 `CacheQuotaData` 仅由周期任务落库，重启最多丢一个 DataExportInterval 窗口的数据。
2. `8874d1929` 中"启动成功日志先于端口监听打印"的问题 fork 同样存在（`internal/app/server.go:154` 在 `server.Run` 前调用 `LogStartupSuccess`，端口占用时先报成功再 Fatal）。该提交的另一子项（quota 日志同步化）不适用：fork 的消费日志路径有自有设计注释且 `LogQuotaData` 仅内存 append。

应当表现为：
1. `http.Server` + goroutine `ListenAndServe` + `signal.Notify(SIGINT/SIGTERM)` + 带超时的 `Shutdown`（超时建议默认 120s、保留环境变量覆盖并同步 `.env.example` 与中英文环境变量文档）；退出前 DataExportEnabled 时调 `SaveQuotaDataCache()`；`LogStartupSuccess` 移到监听启动之后；`cmd/server` 维持只处理退出码。

---

#### 【P1】`153d7f01a` 流式响应补 per-write 超时上限（主体缺陷 fork 架构免疫，两个子项仍适用）

涉及文件：
1. `relay/helper/stream_scanner.go` + `relay/helper/common.go` → `internal/relay/helper/`

原因：
1. fork 无独立 ping 保活 goroutine（ping 内联于 scanner 同一 select 循环），客户端断连时主循环已能返回并 defer 关闭，上游 wg.Wait 挂起/stale write 主体缺陷不存在；StringData/PingData/FlushWriter 也已有 ctx-done 错误契约。
2. 仍缺：①fork 无任何写超时（`internal/app/server.go:156` 的 http.Server 无 WriteTimeout，全仓无 `NewResponseController|SetWriteDeadline`），慢而连着的客户端可无限阻塞 handler goroutine 并占住中继并发槽；②`internal/relay/helper/common.go:58-80` 的 `ResponseChunkData`/`ClaudeChunkData`/`ClaudeData` 不检查 ctx-done，前两者无返回值、`ClaudeData` 虽有 error 签名但恒返 nil。

应当表现为：
1. 流写前设置 per-write deadline（`http.NewResponseController`，30s 上限，best-effort）；`ResponseChunkData` 返回 error、ctx-done 时报错；附 scanner 测试。

---

### 用户 / 账户

#### `bed4a3f91` 用户名注册与更新 trim 空白并拒绝空串

涉及文件：
1. `controller/user.go` → `internal/controller/user.go`（Register:161-194 / UpdateUser:515-542 / UpdateSelf）

原因：
1. fork 的注册与管理员更新用户名均无 `strings.TrimSpace` + 空值拒绝，而登录端 `ValidateAndFill` 会 trim（`internal/model/user.go:503`），可造成"看起来同名实不同"的账号。注意 fork 管理员更新走 `UpdateUserRequest` 结构，需按 fork 结构适配。

应当表现为：
1. Register/UpdateUser（及如允许改名的 UpdateSelf）对 username 做 TrimSpace 并拒绝空串，与登录侧一致。

---

### 前端修复

#### 【P1】`394b023db` 分组倍率输入无法键入小数（`0.05` 被塌缩成 `0`）

涉及文件：
1. `group-ratio-visual-editor.tsx` → `web/src/features/system-settings/models/group-ratio-visual-editor.tsx`（:73/:93-96/:818/:904-916）

原因：
1. fork 保留上游修复前完全相同代码：`ratio: number` + 每次 keystroke `normalizeRatio()` 归一化 + `value={String(row.ratio)}`，输入 `0.05` 时小数点被 `Number()` 吃掉。

应当表现为：
1. `ratio` 改 string 草稿，仅序列化时转 number。fork 该文件与上游 rc.13 结构一致，可近乎原样移植。

---

#### 【P1】`759ab6bbc` 同路由切子 tab 不再 remount 丢状态

涉及文件：
1. `components/page-transition.tsx` → `web/src/components/page-transition.tsx:59-61`

原因：
1. fork 的 `AnimatedOutlet` 仍按 `pathname` 作 key，而 fork 大量使用 `$section` 参数路由（dashboard/models/usage-logs/system-settings 等），切子 tab 即 remount，时间范围等筛选状态丢失。与上游修复前逐字相同。

应当表现为：
1. 一行改动：`select: (s) => s.matches.at(-1)?.routeId ?? s.location.pathname`，可原样套用。

---

#### 【P1】`df5ba9fa5` 登录表单移除密码 min(8) 客户端门槛

涉及文件：
1. `features/auth/constants.ts` → `web/src/features/auth/constants.ts:25-31`

原因：
1. fork `loginFormSchema` 仍有 `.min(8)`，而后端登录无密码最小长度校验——存量短密码用户会被前端直接拦死无法登录。fork 的用户管理抽屉已有 8-20 校验且 i18n key 化（那部分已等价）。

应当表现为：
1. 登录 schema 仅保留非空校验，长度校验交给服务端。

---

#### 【P1】`25f998595`（部分）JsonEditor 首帧空行 + FixAbilities 确认弹窗

涉及文件：
1. `components/json-editor.tsx` → `web/src/components/json-editor.tsx:66,90-96`
2. `channels-primary-buttons.tsx` → `web/src/features/channels/components/channels-primary-buttons.tsx:168-180`

原因：
1. JsonEditor 挂载时 `value !== jsonValue` 守卫导致首帧不解析——对挂载即携带非空初值的调用方（如 `prefill-group-form-drawer.tsx:351` 按类型条件挂载）可视化模式显示空行，用户可能误以为无配置而误覆盖；渠道编辑抽屉因先以空默认值挂载、数据后经 `form.reset` 到达而恰好绕过。上游修复为 `useState(() => parseJsonRows(value))`，1:1 适用。
2. fork 的"修复渠道一致性"（FixAbilities，重建路由索引）点击直接执行、无确认，上游加了 ConfirmDialog。
3. 该提交其余部分（batchMode/卡片视图/channel-mutate-drawer 重排）fork 结构不同，不适用。

应当表现为：
1. JsonEditor 打开即回显已有键值行；FixAbilities 前弹确认框。

---

#### 【P1】`966af88ec` Playground 两个实质缺陷（巨型重构，按功能点移植）

涉及文件：
1. `features/playground/` → `web/src/features/playground/hooks/use-chat-handler.ts`、`use-stream-request.ts`、`lib/storage.ts`

原因：
1. 非流式请求无中止能力：fork `stopGeneration` 仅断开 SSE（`use-stream-request.ts` 的 `stopStream` 只 `sseSourceRef.current.close()`），`use-chat-handler.ts:138` 的 `sendNonStreamingChat` 无 AbortController。
2. localStorage 恢复无校验：`lib/storage.ts` 裸 `JSON.parse`，无版本/结构校验与历史上限。
3. 上游的 Markdown 渲染改进部分 fork 路径不同（用 streamdown，自带 sanitize），无需跟随。

应当表现为：
1. 非流式请求支持中止；localStorage 恢复前做结构校验并对历史上限截断。

---

#### 【P1】`0b2cf43e7` 个人菜单钱包入口无视侧边栏模块开关

涉及文件：
1. `components/profile-dropdown.tsx` → `web/src/components/profile-dropdown.tsx:115-117`
2. `hooks/use-sidebar-config.ts` → `web/src/hooks/use-sidebar-config.ts`

原因：
1. fork 有完整的侧边栏模块开关体系（`isModuleEnabled`/`nav-modules`/管理端配置），但 profile-dropdown 无条件渲染 Wallet 菜单项——禁用钱包模块后该入口仍显示并可跳转。

应当表现为：
1. 基于 fork 自己的 `isModuleEnabled` 封装单路由可见性判断，Wallet 菜单项按结果条件渲染。

---

#### `90fa6fe6b` 钱包转账弹窗遵循配置的配额单位

涉及文件：
1. `wallet/components/dialogs/transfer-dialog.tsx` → `web/src/features/wallet/components/dialogs/transfer-dialog.tsx:60-61,96-108,120`
2. `wallet/constants.ts` → `web/src/features/wallet/constants.ts:61`（`QUOTA_PER_DOLLAR = 500000` 硬编码）
3. `general/quota-settings-section.tsx`（配额输入规范化部分）→ `web/src/features/system-settings/general/`

原因：
1. fork 的转账弹窗与上游修复前逐字一致：最小值/步进/预览全部硬编码 `QUOTA_PER_DOLLAR`，输入直接当 quota 传给 onConfirm；管理员把 `QuotaPerUnit` 配成非 500000 后，转账门槛与换算全部错误，且确认按钮仅以 `transferring` 禁用、无 `canTransfer` 边界校验（可超额提交）。

应当表现为：
1. 从系统配置读 `quotaPerUnit`，输入以货币金额为单位换算为 quota（参照上游 `parseQuotaFromDollars`；fork 的 `lib/currency.ts` 现无该助手，需补），min/max/step 动态化，加 `canTransfer` 校验禁用按钮；i18n 走 fork 的 locales/{en,zh}/ 体系。

---

#### `3a876d6f3` 已登录用户访问注册页跳转 dashboard

涉及文件：
1. `routes/(auth)/sign-up.tsx` → `web/src/routes/(auth)/sign-up.tsx`（含 register.tsx 别名路由）

原因：
1. fork 的 sign-up 路由无 `beforeLoad` 守卫（与上游修复前逐字相同），而 sign-in.tsx 已有同款守卫可参照。

应当表现为：
1. sign-up 加 `beforeLoad`，已登录时 `throw redirect({ to: '/dashboard' })`。

---

#### `c1903607d` 渠道状态筛选跨页导航持久化

涉及文件：
1. `channels-table.tsx` + filter-bar → `web/src/features/channels/components/channels-table.tsx:101-108`、`channels-filter-bar.tsx:68-90`

原因：
1. fork 状态筛选纯 URL 驱动，无 localStorage 持久化（fork 已持久化 tag-mode/id-sort/column-visibility，唯独缺 status）；离开 /channels 返回即丢失。

应当表现为：
1. 状态筛选写 localStorage，filter-bar 初始化回读。fork 无上游 deserialize 钩子机制，需在初始化处实现等价逻辑。

---

#### `2f91d8ccb` 首页 iframe 主题与语言同步

涉及文件：
1. `features/home/index.tsx` → `web/src/features/home/index.tsx:48-52`

原因：
1. fork 的 iframe 模式首页无 ref/onLoad/postMessage，嵌入页无法跟随站内深浅色与语言切换。

应当表现为：
1. 加 `iframeRef` + `resolvedTheme` + `i18n.language` 监听，onLoad 与变化时 postMessage `{themeMode}`/`{lang}`（跨域 try/catch）。

---

#### `2281c9e3d` 移动端用户卡片字段排序

涉及文件：
1. 上游 `data-table/layout/` + `users-columns.tsx` → fork `web/src/components/data-table/mobile-card-list.tsx:48-50,140`、`web/src/features/users/components/users-columns.tsx:81`

原因：
1. fork 移动端卡片按列定义顺序渲染、无 `mobileOrder`；用户卡片 ID 列被隐藏、角色/分组/额度次序不合理。

应当表现为：
1. mobile-card-list 的 meta 加 `mobileOrder` 并排序；users-columns 给 id/role/group/quota 设定顺序。实现位置与上游不同，按 fork 结构重写。

---

#### `f52b52b16` 日志详情弹窗内动态计费 breakdown 紧凑样式

涉及文件：
1. `pricing/components/dynamic-pricing-breakdown.tsx` → fork 同路径；`usage-logs/.../details-dialog.tsx:1409-1413`

原因：
1. fork 的 `DynamicPricingBreakdownProps` 无 `compact`，弹窗内嵌渲染时带彩色图标大标题，与对话框分节样式不协调。

应当表现为：
1. 增加 `compact` prop（去图标头、text-xs、紧凑间距），details-dialog 调用处传入。

---

#### `c5600f9b1` 渠道测试弹窗操作列图标化收敛

涉及文件：
1. `channels/components/dialogs/channel-test-dialog.tsx` → fork 同路径（:236/:517-540）

原因：
1. fork 仍是文本按钮撑宽操作列（`w-24 sm:w-28`），表格宽度被操作列挤占。

应当表现为：
1. 单模型测试按钮改 ghost + icon + Tooltip（fork 已 import Tooltip），操作列宽收敛。需按 fork 已大幅重构的文件写法适配。

---

#### `f9e508bda`（部分）表格内联数值输入超宽截断

涉及文件：
1. `channels/components/numeric-spinner-input.tsx` → fork 同路径（:169）

原因：
1. fork 该组件用于渠道表格内联优先级/权重编辑，超大数值撑破单元格（与上游修复前逐字相同）。

应当表现为：
1. 显示截断到 `max-w-16 truncate` 并加 `title` 提示完整值。该提交其余部分（卡片视图/ChannelCard memo）fork 无对应结构，不适用。

---

#### `8ad83bf62`（两个独立子项）dashboard 筛选弹窗回显已应用条件 + 用户分析筛选跨 section 保留

涉及文件：
1. `dashboard/components/models/models-filter-dialog.tsx:87-102` → fork 同路径
2. `dashboard/components/users/user-charts.tsx:82-89` + `dashboard/index.tsx:265` → fork 同路径

原因：
1. fork 的模型筛选弹窗打开时从 preferences 重置而非回显当前已应用条件；用户分析的时间范围/粒度/TopN 是组件内 state，切 section 即丢。两者均与上游修复前逐行相同，且**独立于 sankey 链可单独移植**。

应当表现为：
1. 筛选弹窗接收 `currentFilters` 初始化；时间范围/粒度/TopN 提升为 dashboard 受控 state。

---

#### `1e80ce03e` 升级前旧记录提示文案更新

涉及文件：
1. `usage-logs/.../details-dialog.tsx:1274` → fork 同路径；i18n key `usageLogs.tips.recordWasWrittenByAPreUpgradeInstanceAnd`

原因：
1. fork 各语言仍是旧文案"建议升级"，对已升级实例有误导。

应当表现为：
1. 按 fork 的 locales/{en,zh}/usage-logs.json 更新为新文案（历史记录无法回填）。

---

## 功能新加

### 渠道 / 运维

#### `efd6c445a` 被动渠道监控模式（passive_recovery）

涉及文件：
1. `setting/operation_setting/monitor_setting.go` → `internal/config/operation/monitor_setting.go:10-13`（仅两字段）
2. `controller/channel-test.go` → `internal/controller/channel_test_handler.go:1630-1654`（`testAllChannels` 定义于 :1539，签名多 `*gin.Context`）
3. 前端 `monitoring-settings-section.tsx` + i18n

原因：
1. fork 的自动渠道测试只有"全量定时测试"一种模式；上游新增 passive_recovery：只重测被自动禁用的渠道、绝不自动禁用渠道，减少主动探活对健康渠道的计费消耗。

应当表现为：
1. 监控设置新增 `channel_test_mode`（scheduled_all/passive_recovery）；passive 模式仅测 ChannelStatusAutoDisabled 渠道。fork 的 testAllChannels 签名多 `*gin.Context`（i18n），需按 fork 结构拆分。

---

#### `44e0e6868` 渠道自动测试环境变量开关

涉及文件：
1. `setting/operation_setting/monitor_setting.go` → `internal/config/operation/monitor_setting.go:27-33`

原因：
1. fork 已支持 `CHANNEL_TEST_FREQUENCY` 覆盖但无 `CHANNEL_TEST_ENABLED` 布尔开关，无法用环境变量强制开/关自动测试。

应当表现为：
1. `os.LookupEnv("CHANNEL_TEST_ENABLED")` + ParseBool 覆盖（失败忽略）；补 t.Setenv 测试。与上条同文件，建议一起落地。

---

#### `2c2d92116` 清理默认倍率表死条目

涉及文件：
1. `setting/ratio_setting/model_ratio.go` → `internal/config/ratio/model_ratio.go`

原因：
1. fork 默认倍率表（约 675 条）仍含上游本轮清除的死条目：360GPT_S2_V9（:27，对应已删除的 ai360 渠道）、babbage/babbage-002/davinci/text-ada-001/text-babbage-001/text-curie-001 等废弃模型。

应当表现为：
1. 移除已删渠道与废弃模型的默认倍率条目，保持兜底行为不变，属表清理无行为风险。

---

### 前端体验

#### Dashboard 流量桑基图四连 `a68041f7d` + `061948011` + `8ad83bf62` + `5e8664464`

涉及文件（整链）：
1. 后端 `controller/usedata.go` + `router/api-router.go`（新增 `/api/data/flow`）→ `internal/controller/usedata.go` + `internal/router/api_router.go`
2. `model/usedata_flow.go`（新）+ `model/usedata.go` + `model/log.go`（QuotaData 加 group/token_id/channel_id 维度）→ `internal/model/`
3. 前端 `dashboard/components/flow/` + `multi-select.tsx` → `web/src/features/dashboard/`

原因：
1. fork dashboard 仅有 overview/models/users 三个 section，无流量桑基图；图表库（@visactor/vchart）与上游一致。四提交为一个功能链：基础图 → 节点上限（防大范围渲染卡死）→ 交互高亮/持久筛选 → 敏感信息脱敏开关。
2. **注意**：需扩展 quota_data 表列（三库迁移），且 fork 有刻意的写入匿名化设计（applyTrackingConfig）——关闭 by_user/by_model 时 flow 维度应同步退化；i18n 按 fork 的 locales/{en,zh}/ 分文件结构。

应当表现为：
1. dashboard 新增 flow section（建议 adminOnly），用户→令牌→模型→渠道流量路径图，节点超限聚合"其他"，头部有脱敏眼睛开关。8ad83bf62 的两个独立子项（筛选弹窗回显、用户分析状态提升）已拆到"修复"节，可先行移植。

---

#### `fc26b88fd` 分组倍率编辑器增强（JSON 静默解析已具备，充值倍率并表 / 特殊分组下拉可移植）

涉及文件：
1. `group-ratio-form.tsx` / `group-ratio-visual-editor.tsx` / `group-special-usable-editor.tsx` → fork 同名三文件（`web/src/features/system-settings/models/`）+ i18n

原因：
1. 上游三项增强中，"解析失败静默回退" fork 已具备：`safeJsonParse`（`web/src/features/system-settings/utils/json-parser.ts`，含 fallback + silent 语义）覆盖了该场景。
2. 剩余差异：①充值倍率（topupGroupRatio）在可视化编辑器中是独立只读单元格列表（`group-ratio-visual-editor.tsx:436-465`），改值需铅笔按钮弹 `SimpleGroupDialog` 或切 JSON 模式，未并入主表格列；②特殊分组规则编辑器已是结构化表单（Select 操作符 + Input，`group-special-usable-editor.tsx:197` 起），但 targetGroup/新分组名字段仍是自由文本，没有已知分组下拉。

应当表现为：
1. 择需移植：特殊分组的分组名字段补已知分组下拉；充值倍率并入可视化主表格列。fork 与上游该组文件结构已大幅分化，不能直接 cherry-pick。

---

### 基础设施

#### `2f23a6673` SMTP STARTTLS 与 NTLM 认证支持

涉及文件：
1. `common/email.go` → `internal/common/email.go:48-91`（仅 465 隐式 TLS 与 smtp.SendMail 两分支，无显式 STARTTLS/NTLM 控制）
2. `common/email_ntlm_auth.go`（新增）→ `internal/common/`（新建）
3. `common/constants.go` → `internal/common/constants.go:72-74`（增补 STARTTLS/InsecureSkipVerify 变量）
4. 前端 SMTP 设置 + i18n（en/zh 两份）

原因：
1. fork 的 SMTP 仅 465 隐式 TLS（`tls.Dial`，`:49-52` 硬编码 `InsecureSkipVerify: true`）与非 465 的 `smtp.SendMail` 两分支；标准库虽会在服务器宣告时自动协商 STARTTLS，但无显式加密模式控制、无 NTLM，要求显式 STARTTLS/NTLM 的 587 Exchange 场景不可用，且 SMTP 无独立 skip-verify 开关（HTTP 的 `TLS_INSECURE_SKIP_VERIFY` 不接线到 SMTP）。

应当表现为：
1. `newSMTPClient`（SSL/STARTTLS/明文三模式 + 可配置 InsecureSkipVerify）与 `AutoSMTPAuth`（PLAIN 优先、LOGIN 兼容、NTLM 兜底）；前端加密模式选项。

---

#### `70c0b37ee` 移除零引用的 date-fns 直接依赖

涉及文件：
1. `web/package.json`（dependencies 删 `"date-fns": "^4.4.0"`）+ `bun.lock`

原因：
1. fork `web/src` 与 scripts 中 grep date-fns 零命中（日历 locale 走 `react-day-picker/locale`，react-day-picker@10 自带 date-fns 传递依赖），直接依赖纯属冗余。

应当表现为：
1. 删除后 `bun install`、`bun run build`、`bun run typecheck` 通过；`bun pm why date-fns` 仅剩 react-day-picker 传递引用。

---

#### `95e8c5eec` Rsbuild/Tailwind 构建管线优化

涉及文件：
1. `web/rsbuild.config.ts`、`web/postcss.config.mjs`（删除）、`web/package.json`

原因：
1. fork 仍走 `@tailwindcss/postcss` + `web/postcss.config.mjs` 的 PostCSS 管线（`package.json` 中 `@tailwindcss/postcss`/`postcss` 均在）；上游改用 `@rsbuild/plugin-tailwindcss`（Rsbuild 2 官方插件，Lightning CSS 管线）提升构建速度。

应当表现为：
1. Tailwind 编译改走 Rsbuild 插件管线。注意上游同时调整了 buildCache 策略，需结合 fork CI 缓存实测 CSS 产物无陈旧后取舍。

---

## 战略级（大改，建议单独分支，勿直接 cherry-pick）

### ClickHouse 日志库链 `6dc4030fd` + `f84b7d591` + `df44a75d5`

涉及文件：`common/database.go`、`model/main.go`、`model/log.go`、go.mod（clickhouse 驱动）等整链。

现状：fork 仅存死变量 `UsingClickHouse = false`（`internal/common/database.go:13`），日志库仅 SQLite/MySQL/PG；LIKE 均为 `ESCAPE '!'` 三库兼容写法、清理为同步端点 + 分批删除。

应当表现为：若决策引入，必须整链移植（类型化 DatabaseType、clickhouse DSN、排序/request_id 兜底/TTL/统计适配、单次 mutation 删除、LIKE 反斜杠转义），不能只拿补丁。fork 现有三库承诺与 `.env.example` 文档需同步。**纯容量决策，单实例中小部署无痛点。**

---

### 节点/系统任务基建链 `354d0fedb` + `2cbdfa039` + `537719229` + `a72e5082e` + `a162163b4`（+ `f8cfbfa4d`）

涉及文件：`common/node_identity.go`、`model/system_task.go`/`system_instance.go`、`service/system_task.go`、controller/router、前端 `features/system-info/` 等整链。

现状：fork 无 NodeName 概念（仅 NODE_TYPE master/slave）、无 system_task/system_instance 表、无任务运行器；渠道自动测试是主节点 sleep 循环。上游该链迁移的任务源（midjourney 轮询/异步任务轮询/渠道上游更新）fork 均已删除，**实际可接入的只有渠道测试一个，+2561/-582 的移植收益大幅缩水**。

应当表现为：仅当 fork 有多实例横向扩展规划时，作为统一战略分支设计（节点身份 → 实例心跳/面板 → 任务运行器 → 陈旧实例清理），接入点按 fork 代码重写，且遵守 fork 规范（常驻资源保守、RecordAudit、i18n、环境变量文档）。`f8cfbfa4d`（删除旧清理端点）在 fork **不可移植**——fork 的 `DELETE /api/log/` 是唯一清理入口且已审计化。

---

### 细粒度管理员权限（casbin RBAC）`4aee5f7d5`

涉及文件：上游 `service/authz/*`（11 文件）、`model/casbin_rule.go`/`authz_role.go`、`middleware/auth.go`、路由与前端权限编辑器，+2776 行。

现状：fork 仅三角色 + 路由整体挂 AdminAuth/RootAuth；敏感操作已用另一方式加固（如 GetChannelKey 要求 RootAuth+SecureVerification），但"某管理员只授渠道编辑、不给删除/密钥查看"这类粒度做不到。上游提交中的审计归属修正部分对 fork 不适用（fork 自有审计体系操作者归属天然正确）。

应当表现为：若产品需要管理员分权，单独分支引入 casbin（需过三库兼容）、权限注册表、RequirePermission 逐路由映射、前端权限编辑器；属方向性产品决策。

---

## 附录：全部 130 个非 merge 提交判定总表

适用 **48**（含部分适用 8、战略级 5）｜已吸收 **12**｜不适用 **70**。

### 已吸收（12）

| 提交 | 说明 | fork 证据 |
|---|---|---|
| `6ce7305cd` GPT-5.6 倍率 | token ratios | `internal/config/ratio/model_ratio.go:231-238` 数值一致且更全 |
| `70ea899e3` 行锁集中化 | GORM 锁 | fork 直接用 `clause.Locking`，redemption/user/topup 均有 |
| `df087b022` SSRF 防护 | HTTP client | fork 自有更强实现（dial 时复查防 DNS rebinding） |
| `56dbaab1d` Secure cookie | session | `internal/app/server.go:129-138` COOKIE_SECURE |
| `bfddc5fea` access_token 泄露 | user query | `Omit("password","access_token")` + `json:"-"` |
| `2d5a04163` Responses↔Chat | 互转 | `internal/relay/openai_wire_*` 双向状态机覆盖 |
| `3a506f50f` Chat→Responses 兼容 | 协议 | 同上（incomplete reason 细节 fork 不读该字段） |
| `1e11dfcfb` 兑换失败消息 | 兑换码 | 模型层统一 `ErrRedeemFailed`，等价 |
| `9ba251ce5` 表格操作/危险弹窗 | 前端 | overflow 菜单 + destructive confirm 已有 |
| `8bc4bf1d6` cosign 签名 | CI | fork docker-image.yml 已有 cosign + id-token |
| `1dcb389d0` x/image bump | deps | fork v0.45.0 ≥ 0.41.0 |
| `69c4d83df` x/net bump | deps | fork v0.57.0 ≥ 0.55.0 |

### 不适用（70）

**已删除渠道/功能（18）**：`043720f9b`/`52858ad1e`/`e514db20f`/`c8491b41b`/`d10fc762f`/`d2dcbc313`/`b191f4737`（task/ali/doubao 视频任务框架，fork 仅保留 bytedance LLM 透传，框架本体已删净、仅余渠道类型常量与 `GetMidjourney` option 等痕迹）；`4ae341756`（Codex 渠道类型 57）；`0b7ae4ea7`（StepFun 图标条目）；`aa334c085`（ai-elements context.tsx）；`9b93d61b7`（订阅，前端 features/subscriptions 为删除后端时遗留的孤儿代码）；`79396745d`（Waffo，前端 wallet 下仍有死代码待自行清理）；`5d943281b`/`5814ca90c`（用户 token 限额，fork 有 cleanup_removed_token_setting.go 迁移主动删除该 option）；`740917445`（登录日志 type=7，fork 不产生登录日志）；`43591fba7`/`cb841850b`（Advanced Custom 渠道 type 58）；`c0e42bfbd`（classic/新版主题切换）。

**fork 未实现机制 / 架构差异 / 依赖链缺失（24）**：`3fbad6a72`/`48b7f4918`/`bae799ccb`/`fc1259f58`（billingexpr 阶梯计费/饱和审计/OtherRatios，fork 无 billingexpr，OtherRatios 仅有无调用者的死写入方法 `AddOtherRatio`，属休眠代码）；`d0bd8aac7` 余项（task/MJ/ali 视频路径，image N 与 max_tokens 校验部分已吸收）；`4a64b8707`（纯测试，随 `5fc35e28a` 携带）；`cf6ae6fde`（fork 的 PLAIN 认证直接用 smtp.PlainAuth 自带守卫）；`0d5995eb6`（fork `TokenAuthReadOnly` 注释明确有意允许禁用令牌只读，属设计分化）；`3245b2b74`（分层定价编辑器）；`0977965d9`（fork ollama 适配器直接透传 OpenAI 协议，无原生协议转换器）；`de0d6ac99`/`d58029c63`/`0c6c1b373`/`1d166532f`/`91ab664c5`/`1f1da5536`（前端结构/架构差异：fork 无卡片网格、无 saved/draft 双态定价、计费历史走整页表格、无 section 子组件体系、国产模型图标为后端元数据驱动）；`dfcb74b52`/`fae39cd90`（上游 subscription.go 的指针布尔列，fork 无该文件且无同类模式）；`f8cfbfa4d`/`354d0fedb`/`a72e5082e`/`f84b7d591`/`df44a75d5`/`a162163b4`（依赖战略级 ClickHouse/节点任务基建链，见战略级节注，仅随链评估）。

**仅 CI/构建/文档/格式化/electron/classic/上游内部事务（28）**：`5cbb7b0be`、`d1abf78ec`（zh-TW，fork 仅 en/zh）、`45f0484dc`、`a13010394`、`997926bbe`、`917a2cff6`、`55858f353`、`a6c020125`、`bff701b0c`、`e1fd9cc28`、`f9165e7bf`、`5bf346836`、`12fc01006`、`6c35e1ef2`、`c12e5db4f`、`69b0f0b56`、`48da37a3d`、`ad35ab1d9`、`acb52d0f7`、`64eafc941`、`72b3f3457`、`993d67ebd`、`9fc9c8f1e`、`e5694748c`（tsgo 实验性选型）、`becc18e30`/`8f31b3059`（fork 用标准 `en`/`zh` locale 码 + languageOnly，上游驼峰码缺陷结构性不存在）、`86021d8ed`（实测 15 文件全部位于 web/classic，标题中 "backend sync" 指 classic 前端）、`b35dfa32e`（fork 平行实现无该问题）。

> 注：`d0bd8aac7`、`12603a776`、`25f998595`、`f9e508bda`、`8ad83bf62`、`153d7f01a`、`8874d1929`、`c9943d37a`、`90fa6fe6b` 为部分适用（提交含不适用 hunk），已拆入正文对应条目；战略级链中标注"随链评估"的成员在上表归入"依赖链缺失"。
