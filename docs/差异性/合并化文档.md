# 差异性文档（合并版：v1.0.0-rc.14 ~ v1.0.0-rc.25）

> 时间: 2026-08-20
> 对比上游 NewAPI 现有版本: `v1.0.0-rc.13`（2026-06-19）→ `v1.0.0-rc.25`（`f11641428`，2026-08-18），
> 覆盖 rc.14 ~ rc.25 共 11 个版本、合计 **296** 个 Commit（286 个非 merge）。
> 本文档由两份批次文档合并而成，条目正文保持源文档原样（未按合并时点重新核验，两批次判定均基于
> 2026-08-19 工作区，即 #283/#308 两次上游同步之后）：
>
> - `v1.0.0-rc.14~v1.0.0-rc.20.md`（rc.13→rc.20，含 `v1.0.0-rc.19-i18nfix.1/2` 补丁标签）: 134 个 Commit（130 个非 merge）；
> - `v1.0.0-rc.21~v1.0.0-rc.25.md`（rc.20→rc.25）: 162 个 Commit（156 个非 merge）。
>
> **合并口径**: 同名分级（安全性/P0、安全性/P1、修复/计费协议正确性、功能新加/前端体验）合为一节，
> 节内 rc.14~rc.20 批次条目在前、rc.21~rc.25 批次条目在后（与上游 commit 顺序一致，批次交界处有
> 分界标记行）；仅单一批次使用的分类保留原名，rc.14~rc.20 的分类排在前面。两份源文档对同一事项
> 结论有出入时（如 dompurify 风险口径），在条目旁以「合并注」说明。
>
> 本文档只收录上游对 **本仓库仍保留功能** 的修复 / 新增 / 完善。fork 已通过 2026-08-07（#283）与
> 2026-08-17（#308）两次上游同步吸收了 rc.14~rc.25 期间的大量修复，此处仅收录截至各自核验时点
> 【尚未吸收】的变更。fork 已删除的渠道（kling/jimeng/vidu/sora/coze/dify/xai/volcengine/baidu/ali/
> xunfei/tencent/360/perplexity/cohere/cloudflare/mistral/jina/midjourney/suno/xinference/replicate/
> codex/stepfun/ai360/waffo 等）、已删除功能（electron/订阅/task 异步任务框架/video_proxy/web classic
> 老前端等）以及已吸收提交均不在此列（完整判定见文末两个附录）。
>
> **fork 布局提示**: 本仓库已重构为 `internal/` 布局（模块 `github.com/NookMux/NookMux`）。上游路径
> 对应关系: `common/`→`internal/common/`、`setting/`→`internal/config/`、`relay/`→`internal/relay/`、
> `logger/`→`internal/infra/log/`、`web/default/src/`→`web/src/`。上游在本期间还完成了前端目录
> `web/default/` → `web/src/` 改名、协议转换抽出独立 `relaykit/` 模块等结构重构，协议转换类改动按
> 能力对齐（上游 `relaykit/relayconvert` ≈ fork `internal/relay/helper/convert.go` + `internal/relay/`
> 适配器）。下文"涉及文件"同时标注上游路径与 fork 对应路径。
>
> 核验方法: 两批次各 7 批子代理逐条分析（覆盖全部 286 个非 merge 提交）+ 仓库代码逐一比对确认
> （"已吸收"判定均附 fork 侧文件:行号证据，"缺失"判定均经 fork 侧 grep/读取反证）+ 主代理对
> P0 级结论逐一复核与抽样复核。
>
> 历史说明: 源文档中引用的「v1.0.0-rc.13 差异性文档」已删除（69 项已全部修复关闭），相关引用
> （如 `5238f279`）仅作历史线索。

---

## 目录（正文已拆分为 8 个子文档）

> **拆分说明（2026-08-20）**: 本文档正文已按章节拆分为同目录下 8 个子文档，本文件保留总览、
> 合并口径与拆分导航。各子文档顶部均有返回本文件与互链全部姊妹篇的导航条；条目正文在子文档中
> 保持源文档原样（仅标题层级随独立成篇上提一级）。拆分时将两处跨章节「结论相反且需同批落地」
> 的条目合并为同批条目（见下方「拆分时合并批」），原位置留有并入注，其余条目与源文档一一对应。
> 头部说明中提到的「文末两个附录」指两份源批次文档内的附录（源文档已删除），本次拆分未涉及。

| # | 子文档 | 对应原章节 | 条目数 |
|---|--------|-----------|--------|
| 1 | [安全性](安全性.md) | 安全性（P0 × 6 / P1 × 7） | 13 |
| 2 | [修复](修复.md) | 修复（计费/协议正确性、可靠性/健壮性、用户/账户、前端修复、重试/错误/限流、性能/健壮性、数据库/缓存兼容、渠道/前端修复 共 8 小节） | 61 |
| 3 | [功能新加 · 渠道/运维](功能新加-渠道运维.md) | 功能新加 / 渠道 / 运维 | 3 |
| 4 | [功能新加 · 前端体验](功能新加-前端体验.md) | 功能新加 / 前端体验 | 9 |
| 5 | [功能新加 · 基础设施](功能新加-基础设施.md) | 功能新加 / 基础设施 | 3 |
| 6 | [功能新加 · 分组/计费](功能新加-分组计费配置.md) | 功能新加 / 分组 / 计费配置 | 4 |
| 7 | [功能新加 · relay 能力](功能新加-relay能力.md) | 功能新加 / relay 能力 | 2 |
| 8 | [战略级](战略级.md) | 战略级（大改，建议单独分支，勿直接 cherry-pick） | 5 |

合计 **100** 个条目（拆分前 102 条，两组合并批各减 1 条）。

### 拆分时合并批（结论相反、需同批落地）

子标题由单条 `{commit id}+{title}` 改为 `{commit id 1}+{commit id 2}+{new-title}`：

1. **`0bf42781d` + `35074345e` + `f250f3b58` dompurify 口径统一：传递链 CVE 风险实存，升级或清理须同批处置**
   —— 位于 [安全性](安全性.md) 篇 P1 首条；原 [修复](修复.md) 篇「渠道 / 前端修复」末条 `f250f3b58`
   位置留有并入注。
   「修复」批判定 dompurify 为"残留依赖不进 bundle、实际风险面为零"，与「安全性」批
   `0bf42781d`+`35074345e` 的 CVE 传递链风险判定**相反**；经复核 `web/bun.lock` 的
   `streamdown` → `mermaid` → `dompurify` 传递链确实存在，CVE 升级结论成立。两口径并为一条：
   升级 overrides ≥3.4.11 或 knip 清理，二选一，同批一次处置，不允许两个口径并存。
2. **`efd6c445a` + `5d3423bec` 渠道测试模式体系统一（scheduled_all / auto_ban_only / passive_recovery）**
   —— 位于 [功能新加 · 渠道/运维](功能新加-渠道运维.md) 篇首条；原
   [功能新加 · 前端体验](功能新加-前端体验.md) 篇 `5d3423bec` 位置留有并入注。
   两条对同一 `ChannelTestMode` 字段给出**不一致的枚举设计**（`efd6c445a` 两值 vs `5d3423bec`
   三值）且互为前置依赖，分开落地必然返工；按上游最终态合并为三值枚举一次落地。

### 未合并、但建议一并设计的关联项（保留原「合并注」口径）

- **饱和/上限框架缺口**: [安全性](安全性.md) P0 `47ba9d2c6` / `2a0ce3475` 与 [修复](修复.md) 篇
  `c9943d37a` / `d9595831b`+`621927f71` 共享"fork 无饱和/上限框架"缺口，建议一并设计饱和工具。
- **`User.Update` 全量回写竞态家族**: [安全性](安全性.md) 篇 `dfc0d6324` / `0cd9dc85e` 与
  [修复](修复.md) 篇 `d7992672a` 同根源，修复时应统一收口（定向更新列 / Omit 并发敏感字段），
  而非逐调用点打补丁。
- **渠道测试体系**: [修复](修复.md) 篇 `4add708eb`（并发执行）与上述合并批 2 同属渠道测试体系，
  建议一并设计。
- **HTTP transport 缓存键**: [功能新加 · relay 能力](功能新加-relay能力.md) 篇 `e99a9bd86`
  前置于 [修复](修复.md) 篇 `e13d4033e` 的缓存键重构，应先吸收后者。
